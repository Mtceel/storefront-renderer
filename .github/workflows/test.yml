name: Test Build

on: workflow_dispatch

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "Short SHA: $(echo $GITHUB_SHA | cut -c1-7)"
          ls -la
          cat package.json | grep cors
